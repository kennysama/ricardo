import React from 'react';
import styles from './ClientHeader.module.scss';

const ClientHeader: React.FC = () => (
  <div className={styles.clientHeader}>Header</div>
);

export default ClientHeader;
